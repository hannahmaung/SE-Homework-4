#Program that generates a full name when provided the first and last name as inputs 

def name(first,last):
    if(name == ' '):
        print("Empty string")
    else:
        return first + " " + last
